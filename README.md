# How to Run

## 1. Install dependencies
pip install scikit-learn ucimlrepo

## 2. Run the script
python main.py config.json if no file is specified, it defaults to config.json

## Output
The script prints cross-validated Accuracy, Macro F1, and Macro Recall for each model.
